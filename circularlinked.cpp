#include <bits/stdc++.h>
using namespace std;
class Node
{
public:
    int data;
    Node *next;
};
void push(Node *head)
{
    int value;
    cin >> value;
    Node *ptr = new Node();
    ptr->data = value;
    if (head != NULL)
    {
        }
}
int main()
{
    Node *head = NULL;
    push(head);
    return 0;
}